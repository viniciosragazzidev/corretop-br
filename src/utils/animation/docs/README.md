# Animation

Tokens, variants e hooks de motion compartilhados. Toda animação nova deve
respeitar `prefers-reduced-motion`, usar os tokens existentes e seguir a skill
`transitions-dev`.

Os arquivos atuais permanecem como implementação legada nesta primeira etapa;
os novos módulos funcionam como ponto central de importação durante a migração.
