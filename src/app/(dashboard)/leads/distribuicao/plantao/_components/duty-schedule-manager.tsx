"use client";

import { useActionState, useEffect } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Badge } from "@/components/ui/badge";
import { createDutyScheduleAction, toggleDutyScheduleAction, type DutyActionState } from "@/features/lead-distribution/duty-actions";

type Branch = { id: string; name: string }; type Queue = { id: string; branchId: string; name: string }; type Schedule = { id: string; branchId: string; queueId: string; name: string; dayOfWeek: number; startsAt: string; endsAt: string; priority: number; status: string };
const days = ["Domingo", "Segunda", "Terça", "Quarta", "Quinta", "Sexta", "Sábado"];
function Feedback({ state }: { state: DutyActionState }) { useEffect(() => { if (state.success) toast.success("Plantão atualizado."); if (state.error) toast.error(state.error); }, [state]); return null; }
export function DutyScheduleManager({ branches, queues, schedules }: { branches: Branch[]; queues: Queue[]; schedules: Schedule[] }) { const [state, action, pending] = useActionState(createDutyScheduleAction, {}); const [toggleState, toggleAction] = useActionState(toggleDutyScheduleAction, {}); const defaultBranch = branches[0]?.id ?? ""; const branchQueues = queues.filter((queue) => queue.branchId === defaultBranch); return <div className="grid gap-6 xl:grid-cols-[minmax(0,22rem)_minmax(0,1fr)]"><Card className="h-fit shadow-none"><CardHeader><CardTitle>Novo plantão</CardTitle><CardDescription>Use prioridade menor para a regra que deve vencer em conflitos.</CardDescription></CardHeader><CardContent><form action={action} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="duty-name">Nome</Label><Input id="duty-name" name="name" placeholder="Plantão comercial" required /></div><div className="grid gap-2"><Label htmlFor="duty-branch">Unidade</Label><select className="h-9 rounded-lg border border-input bg-background px-3 text-sm" defaultValue={defaultBranch} id="duty-branch" name="branchId">{branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name}</option>)}</select></div><div className="grid gap-2"><Label htmlFor="duty-queue">Fila</Label><select className="h-9 rounded-lg border border-input bg-background px-3 text-sm" defaultValue={branchQueues[0]?.id} id="duty-queue" name="queueId">{branchQueues.map((queue) => <option key={queue.id} value={queue.id}>{queue.name}</option>)}</select></div><div className="grid grid-cols-3 gap-2"><div className="grid gap-2"><Label htmlFor="duty-day">Dia</Label><select className="h-9 rounded-lg border border-input bg-background px-2 text-sm" defaultValue="1" id="duty-day" name="dayOfWeek">{days.map((day, index) => <option key={day} value={index}>{day}</option>)}</select></div><div className="grid gap-2"><Label htmlFor="duty-start">Início</Label><Input id="duty-start" name="startsAt" type="time" defaultValue="09:00" required /></div><div className="grid gap-2"><Label htmlFor="duty-end">Fim</Label><Input id="duty-end" name="endsAt" type="time" defaultValue="18:00" required /></div></div><div className="grid grid-cols-2 gap-2"><div className="grid gap-2"><Label htmlFor="duty-priority">Prioridade</Label><Input id="duty-priority" name="priority" type="number" defaultValue="100" min="1" /></div><div className="grid gap-2"><Label htmlFor="duty-valid-from">Vigência</Label><Input id="duty-valid-from" name="validFrom" type="date" required /></div></div><Button disabled={pending} type="submit">Criar plantão</Button><Feedback state={state} /></form></CardContent></Card><Card className="shadow-none"><CardHeader><CardTitle>Plantões cadastrados</CardTitle><CardDescription>Conflitos de prioridade são bloqueados e alterações ficam auditadas.</CardDescription></CardHeader><CardContent className="space-y-2">{schedules.length ? schedules.map((schedule) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-border p-3" key={schedule.id}><div><p className="font-medium">{schedule.name}</p><p className="text-xs text-muted-foreground">{branches.find((branch) => branch.id === schedule.branchId)?.name ?? "Unidade"} · {days[schedule.dayOfWeek]} · {schedule.startsAt}–{schedule.endsAt} · prioridade {schedule.priority}</p></div><form action={toggleAction}><input name="scheduleId" type="hidden" value={schedule.id} /><Button size="sm" type="submit" variant="outline">{schedule.status === "active" ? "Desativar" : "Ativar"}</Button></form><Feedback state={toggleState} /></div>) : <p className="rounded-lg border border-dashed border-border p-8 text-center text-sm text-muted-foreground">Nenhum plantão cadastrado.</p>}</CardContent></Card></div>; }
